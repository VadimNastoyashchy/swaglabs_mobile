describe("Log in tests", () => {
    // beforeEach(async () => {
    //     await $("~open menu").click();
    //     await $('//*[@text="Log In"]').click();
    // });

    it("", async () => {
        await $("~test-Username")
    });
});